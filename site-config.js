/*
 * 公開後にApp Store URLが確定したら、この1か所だけを変更します。
 * 例: appStoreUrl: "https://apps.apple.com/jp/app/..."
 */
window.HIRO_APP_WORKS_CONFIG = Object.freeze({
  apps: Object.freeze({
    consignmentNote: Object.freeze({
      appStoreUrl: "",
    }),
  }),
});
